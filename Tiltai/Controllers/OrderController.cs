
using Microsoft.AspNetCore.Mvc;
using programavimoTiltas.Data;
using System.Linq;

namespace programavimoTiltas.Controllers
{
    [Route("api/[controller]/[action]")]
    public class OrderController : Controller
    {
        //[HttpGet]
        //public IQueryable<Order> GetAll()
        //{
        //    DataModelRepository dataModelRepository = new DataModelRepository(new DataModelContext());

        //    return dataModelRepository.GetOrders();
        //}

        //[HttpPut]
        //public void SaveOrder([FromBody]Order order)
        //{
        //    DataModelRepository dataModelRepository = new DataModelRepository(new DataModelContext());

        //    dataModelRepository.SaveOrder(order);
        //}
    }
}